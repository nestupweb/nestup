# NestUp — accumulated handoff notes

Living list of non-blocking findings from task reviews. Address at the flagged task, or fold into the docs phase.

## For later code tasks

- **PRODUCT CHANGE (2026-08-24, user-approved): pre-match chat.** Chat no longer requires a mutual match — `conversations` table (migration 0004; unique listing+seeker), messages re-keyed to `conversation_id`, messages RLS now conversation-based (match-based policies dropped). Tasks 17/18 (matches flow) must REUSE conversations rather than create a parallel messages path. Still pending: owner inbox (owners currently see "This is your listing" at their own chat URL) and realtime message subscription — chat updates only via revalidation on send this round.
- **Saved rooms (browse heart button):** DONE 2026-08-25 — signed-in users persist to `saved_listings` (migration 0007, `app/actions/saved.ts`), shown under Profile › Liked. Visitors still get the localStorage list (`nestup:saved-listings`); it is NOT migrated into the account on sign-up.
- **Swipe deck (2026-08-25, replaces Task 16 stub):** `app/(app)/swipe/page.tsx` → `lib/swipe.ts` (`getSwipeDeck`: active rooms minus own/already-swiped, owners + `listing_residents`, scored with `lifestyleScore`/`socialScore`, filtered to combined `sortKey` ≥ `MIN_DECK_SCORE` (60) since 2026-08-25, then sorted by `sortKey`) → `components/swipe/{SwipeDeck,SwipeCard,SwipePanel}.tsx`. One room at a time: full-bleed photo story (segment indicators, arrows, ← → keys), Lifestyle/Social pills top-left (`role="img"` + aria-label, so the “add interests” hint is reachable), X / ♥ bottom-right, three-page panel (Essentials · The home · Roommates) with tabs, chevrons and touch swipe. `recordSwipeAction` persists without redirect (a like also upserts `saved_listings` so Profile › Liked agrees); the card animates out via `.swipe-exit-*` in `globals.css` and the next mounts on a 360 ms timer. Listings now need ≥3 photos (`MIN_LISTING_PHOTOS`, enforced in `saveListingAction`; seed rooms backfilled to 3). `components/ui/ScoreTag.tsx` was removed (unused).
- **Storage backstop:** `lib/storage.ts` trusts client `file.type`. Set `allowed_mime_types` on `avatars` / `listing-photos` buckets server-side (small migration).
- **Auth polish (from Task 9 quality review, all Minor):**
  - `lib/auth.ts` `getOwnProfile` discards the `.maybeSingle()` error — a transient DB failure reads as "no profile" and bounces an existing user into onboarding. Destructure `error` and throw it. Trailing `?? null` is redundant.
  - `components/auth/AuthForm.tsx` — React 19 clears uncontrolled inputs when an action returns an error; echo submitted email back via state and `defaultValue` so users don't retype it.
  - `app/actions/auth.ts` — credential extraction duplicated between signUp/signIn (small helper or zod schema); malformed-email branch in signIn returns inaccurate copy ("Email and password are required.").
  - `lib/auth.ts` helpers gained first consumers in Tasks 10–11 (requireUser via /swipe stub + profile action; getOwnProfile via /profile page) — behavior now exercised.
- **Navigation (2026-08-25):** `components/ui/TabBar.tsx` replaced by the floating `components/ui/BottomNav.tsx` (Swipe · Listings · Chat · Profile) rendered by both the (app) and (public) layouts for signed-in users; the Chat item carries the unread badge from `my_unread_count()`. It hides itself on small screens inside `/chat/[id]` so the composer owns the bottom edge. `/listing` (create/edit) is reached from Profile ("+" and the dashed tile).
- **Chat (2026-08-25):** WhatsApp-style inbox at `/chat` + `/chat/[id]` (`app/(app)/chat/*`, `components/chat/*`). `/browse/[id]/chat` is now only an entry point that finds-or-creates the conversation and redirects. Inbox rows come from the SQL function `my_conversations()` (security invoker); unread = messages from the other side newer than `conversation_reads.last_read_at`, stamped by `mark_conversation_read()` (DB clock — an app-clock timestamp lost a race against a same-instant message in the live check). Realtime: `ChatRealtime` subscribes to `messages` + `viewings` and calls `router.refresh()`; chat day labels are rendered client-side only (`useMounted`) to avoid server/client timezone hydration mismatches.
- **Viewings + Google Calendar (2026-08-25):** `viewings` table (proposed/confirmed/declined/cancelled) rendered inline in the thread. `proposeViewingAction` creates a Google Calendar event with both participants as attendees when the proposer has connected Google (`google_tokens`, OAuth routes under `app/api/google/*`, helpers in `lib/google.ts`). Without `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` the UI degrades to a pre-filled "Add to Google Calendar" link (`lib/calendar.ts`). The counterpart's email for the invite is resolved by `conversation_partner_email()` (security definer, participants only) — this intentionally reveals a participant's sign-in email to the other party via the invite. Not yet wired on Vercel: the two Google env vars (and `NEXT_PUBLIC_SITE_URL` for the OAuth redirect) must be added in the Vercel project settings once a Google Cloud OAuth client exists.
- **Household chat + like → hello (2026-08-25):** migration `0008_household_chat.sql` makes a listing's chat a group thread: `is_household_member(listing)` (host or `listing_residents`) joins the conversations SELECT policy, messages policies delegate to it, and `my_conversations()` now returns `household jsonb` (host first, then roommates) — `ConversationSummary.household`. `householdLabel()` (`lib/chat-format.ts`) titles the seeker's inbox/thread ("Dana, Noa & Omer"); bubbles from the household side carry the sender's first name. On Swipe, a like opens `components/swipe/IntroSheet.tsx` with `introMessage(entry)` pre-filled (editable, optional); `sendIntroAction` finds-or-creates the thread and posts it. Gotcha: `IntroSheet` and `SwipeCard` are siblings — never give them the same `key` (a duplicate key kept the outgoing card mounted).
- **Listing form v2 (2026-08-25):** no hand-written title — `buildListingTitle()` (`lib/listing-title.ts`) derives one from type/rooms/area. Address is structured (`street`, `house_number`; `address` stays the composed display string). New columns via `0009_listing_form_v2.sql`: `safe_room` (none/apartment/building — "mamad"), `food_restrictions`, `photo_labels` (a `PhotoRoom` per photo), and photos are now 3–10. `saveListingAction` rejects a listing without living room + bedroom + bathroom photos (`missingPhotoRooms`). `components/listings/PhotoPicker.tsx` keeps the real `photos` file input in sync via `DataTransfer` and submits `existing_photos/existing_labels` + `new_labels`. Cities: `lib/cities.ts` is the national list (`CITIES` re-exported from constants) with `suggestCities`/`matchCity`; `components/ui/CityCombobox.tsx` is the type-ahead used by the listing form, the browse filter and `CityMultiPicker` (profile preferred cities). Seed listings have no photo labels yet — their owners will be asked to tag photos on first edit.
- **Profile photo (2026-08-25):** `/profile/edit` shows the picture as a single circle (`components/profile/ProfilePhotos.tsx`, tap → file picker, previews in place). A short-lived extra-photos gallery (`0010_profile_photos.sql`) was reverted by `0011_drop_profile_photos.sql` at the user's request — profiles keep one `avatar_url`.
- **About me tab (2026-08-25):** first tab on `/profile` (`components/profile/AboutMe.tsx` inside `ProfileTabs`, headers restyled to the Swipe panel's uppercase tab treatment). Private fields live in `profile_details` (`0012_profile_details.sql`, owner-only RLS: about, languages, diet, pet details, lifestyle, wake/bed time, shabbat, cooking, phone, contact email, socials); occupation/smoker/has_pet/budget/move-in are edited there too but stay on `profiles`. `saveAboutAction` (`app/actions/about.ts`) writes both. `?tab=about|listings|liked|history` picks the initial tab (default about).
- **Publishing a listing (2026-08-25):** photos no longer travel through the server action (Vercel's ~4.5 MB body cap silently killed publishes with phone photos — no upload ever reached `listing-photos`). `components/listings/PhotoPicker.tsx` compresses each picture in the browser (`lib/image-client.ts`, ≤1600px JPEG) and uploads it straight to `listing-photos/{userId}/…` with the user's session (storage RLS), so the form submits only `existing_photos` + `existing_labels`; `photos_uploading` blocks a submit mid-upload. `saveListingAction` revalidates /browse, /profile, /swipe and, for a new listing, redirects to `/profile?tab=listings&published=1` (banner). A throwaway account `nestup.tester@nestup.dev` exists for end-to-end publish checks (`scratchpad/make-tester.mjs` resets it).
- **History tab:** `listing_views` is upserted from the listing detail page render for signed-in users (`app/(public)/browse/[id]/page.tsx`); capped at the 30 most recent on the profile.
- **Cosmetic (chat action):** `app/actions/chat.ts` revalidates `/browse/${listingId}/chat` from a client-supplied (UUID-validated) listing_id; no security impact (RLS governs the insert), but could derive the path from the conversation's own listing_id for consistency.

- **City picker browse-all (2026-08-25):** `components/ui/CityCombobox.tsx` (listing form, browse filter, profile `CityMultiPicker`) now has two ways in: typing lists *every* match (was capped at `suggestCities` default 8 — the default stays 8 for `cities.test.ts`; the combobox passes `CITIES.length`), and a chevron button (`aria-label` "Browse all cities") / focusing the empty box lists all 124 cities A–Z with sticky letter headers (`role="presentation"` rows, `aria-hidden` headers, options keep `id=<list>-<index>` for `aria-activedescendant`). `browseAll` state is cleared on typing/Escape/outside click; the active row `scrollIntoView`s (guarded — jsdom lacks it) and the list snaps to the top when its contents change. List is `z-50` so it paints over the `z-40` BottomNav. Tests: `tests/unit/city-combobox.test.tsx`.

- **Page transitions (2026-08-25):** route changes animate with the View Transitions API through React's `<ViewTransition>` (App Router ships React canary; types via `types/react-canary.d.ts` → `react/canary`). `components/ui/PageTransition.tsx` sits in the ROOT layout (`app/layout.tsx`) keyed on `usePathname()` — it must stay at the root: inside the `(app)`/`(public)` layouts a Browse ↔ Profile navigation (layout swap) produced no transition at all. `BottomNav` links pass `transitionTypes={navTransitionTypes(pathname, href)}` (`lib/nav-direction.ts`: tab order Swipe · Listings · Chat · Profile → `nav-forward`/`nav-back`, `[]` otherwise); the active highlight is now a named `nav-active-pill` span so the browser glides it between tabs. CSS in `globals.css`: `.nav-forward`/`.nav-back` slide 22% + fade (0.34 s), `.page-fade` crossfade for untyped navigations (cards, back button), `site-header`/`bottom-nav` pinned (`animation: none`, old snapshot hidden), `::view-transition { pointer-events: none }`, reduced-motion zeroes durations. `router.refresh()` (chat realtime) never animates (`default="none"`). Verified with a Playwright probe that wraps `document.startViewTransition` (types + animated pseudo-elements) — pattern in Temp scratchpad `transition-probe.mjs`. Firefox < 144 / old Safari: no animation, plain swap. Tests: `tests/unit/nav-direction.test.ts`.

- **Instant tab switching (2026-08-25):** measured on production, Profile/Chat/Swipe took 500–1700 ms from tap to any paint (fully dynamic pages, nothing to show until the server answered) while Listings took ~60 ms because `browse/loading.tsx` exists — Next prefetches nav links down to the first loading boundary. Fix: `app/(app)/loading.tsx`, a CLIENT loading boundary at the route-group level that picks a skeleton by `usePathname()` (swipe / chat / profile / generic). Keep it at the group level: a `chat/loading.tsx` would sit under `chat/layout.tsx` (which loads the inbox) and the prefetch would cache that layout for up to 5 min → stale inbox; at the group level only the (app) layout (header + unread badge) is prefetched. After: first paint 64–138 ms for all four tabs; content still streams in 0.5–3 s later (server render — unchanged). Transition CSS shortened to 0.2 s slide / 0.1 s old fade / 0.2 s pill. Timing probe pattern: Temp scratchpad `nav-timing.mjs` (click → `location.pathname` + `main/[aria-busy]` present; settled = no `[aria-busy=true]`).

- **Room page skeleton (2026-08-25):** `app/(public)/browse/loading.tsx` is now a client component that switches on `usePathname()` — the listings grid skeleton for `/browse`, a gallery (16/10) + title + detail-grid skeleton for `/browse/[id]`. Before, opening a card showed the filter-sidebar/cards skeleton under the room URL for ~1.9 s (that boundary covers the child segment too, and a nested `[id]/loading.tsx` would only appear after a round trip). Same pattern as `app/(app)/loading.tsx`.

- **Swipe like order + shorter hello (2026-08-26, user-requested):** a ♥ now persists the swipe immediately but keeps the card on screen under the `IntroSheet`; the `.swipe-exit-like` slide and the next room happen only when the sheet closes (`SwipeDeck` `closeIntro` → `leave("like")`; `decide` ignores input while a sheet is open). Skips are unchanged. `introMessage()` is one line: "Hi Dana [and everyone]! I liked your room at <address, city> — could I come see it?". Tests updated in `swipe-deck.test.tsx` / `swipe-rank.test.ts`.

- **Swipe hello v3 + saved default (2026-08-26, user-requested):** default text is now `DEFAULT_INTRO` = "Hi {name}, I liked the room — can we schedule a viewing?" (`lib/swipe-intro.ts`, `renderIntro(template, hostName)` fills every `{name}` with the host's first name). Seekers can keep their own wording: migration `0017_intro_template.sql` adds `profile_details.intro_template text not null default '' (≤500)` — owner-only via the existing RLS and deliberately NOT in `public_profile_details()`. Edited in two places: the IntroSheet's "Save as my default" link (`saveIntroTemplateAction`, appears once the text differs from the current default) and Profile › edit › **Swipe › Default hello message** (`AboutFields`, `aboutDetailsSchema`/`aboutSchema` + `aboutDetailsFromForm`). `/swipe` page reads the template and passes `introTemplate` → `SwipeDeck` → `IntroSheet`. Sending now closes the sheet immediately and the card slides right (the "Sent / Open the chat" state is gone); the deck's `closeIntro` handles both send and Not now. `ProfileDetails` interface was declared twice in `lib/types.ts` — deduped. Tests: `swipe-rank.test.ts`, `swipe-deck.test.tsx` (send → no dialog → next room; saved template + Save-as-default), `about.test.tsx` literal updated.

- **Member page listing tile (2026-08-26, user-requested):** `/people/[id]` heads the section "<First>'s listing(s)" (was "room(s)") and the `PropertyTile` there shows the photo tagged **bedroom** — the room for rent — instead of the cover (`lib/room-photo.ts` `roomPhoto(listing)` → `{url, isBedroom}`; `PropertyTile` gained a `cover` prop; badge "The room" when a bedroom photo exists). Seed rooms have no `photo_labels`, so they fall back to the cover — only listings published/edited through the v2 form (which requires living room + bedroom + bathroom tags) show the bedroom. Follow-up (same day): the section also lists rooms the member LIVES in (`listing_residents.resident_id = id`, active, deduped against owned, listed BEFORE the rooms they host — seed users are both owners and roommates, and the visitor arrives from a specific post), so every member of a household — host and roommates alike — shows the same tile with the same bedroom photo. Follow-up 2 (user re-checked, 2026-08-26): exactly ONE tile per member — the Roommates tab (`SwipePanel`) and "Who lives here" (`browse/[id]`) link to `/people/<id>?listing=<listingId>`; the page shows that post when the member hosts or lives in it, else the first room they live in, else the first they host. Heading is always "<First>'s listing". Test: `tests/unit/room-photo.test.ts`.

- **Wording: "roommates", never "roommates" (2026-08-26, user-requested):** the listing page spec line now reads "N roommate(s)" (`browse/[id]/page.tsx`); seed copy in `scripts/seed-data.ts` changed, and the live rows were patched in place via SQL (`listings.description` ×2, `profiles.bio` ×7) so no reseed was needed. Code comments/identifiers still say roommate in places — fine, not user-visible.

- **Profile header button (2026-08-26, user-requested):** the top-right "+" (→ `/listing`) on `/profile` is now an **Edit profile** pill (→ `/profile/edit`). Creating a listing is reached only from the My listing tab's "Add listing" tile (`components/profile/MyListing.tsx`). The avatar's pencil badge still links to `/profile/edit` too.

- **Member page = whole post; no avatar pencil (2026-08-26, user-requested):** `/people/[id]` now renders `components/listings/ListingPhotoGrid.tsx` for the one listing it picks (same selection rule as before: `?listing=` → lived-in → owned): every photo in a 2/3-column grid, the room name from `photo_labels` under each (untagged photos get no caption), title + rent, and a "View listing" button → `/browse/[id]`. The previous single bedroom tile (`lib/room-photo.ts`, PropertyTile `cover`) was removed. `ProfileAvatar` lost its hover pencil — the picture only zooms; editing is the header's "Edit profile" button (the no-photo placeholder still links to the editor).

## For the scale doc

- Unbounded page offsets in listing pagination (deep OFFSET scans).
- `messages.sender_id` FK is unindexed.
- UUIDv4 PKs (index locality tradeoff).
- Partial unique index `one_active_listing_per_owner` tradeoffs.
- Supabase dev mailer rate limits (email confirmation at scale needs custom SMTP).

## For the security doc

- `respond_to_interest` executable by any authenticated user is INTENTIONAL (RLS + function logic scope what it can do); anon execute was revoked in migration 0002.
- Supabase advisor `rls_auto_enable` warning is a false positive (event-trigger return type; not directly callable).
- Open-redirect fix history: login/onboarding `next` param sanitized via `lib/redirect.ts` `sanitizeNextPath` — blocks `//` and `/\`, and (commit 7591a6d, after the chat security review) strips ASCII tab/newline/CR first because browsers ignore those in URLs (`/\t/evil.com` would otherwise escape). Unit-tested (11 cases) — good security-doc example of iterative hardening.
- Chat RLS verified by live adversarial probes (2026-08-24 security review): 12/12 spoof/leak attempts blocked (foreign reads, spoofed seeker_id/sender_id, foreign-conversation inserts, own-listing chat, inactive-listing chat, UPDATE/DELETE absence). Pre-existing advisor note: "leaked password protection disabled" (project auth setting) — consider enabling before deploy.
- Client MIME trust in uploads (see storage backstop above).

## Product/state facts

- **Seed data:** `npm run seed` (`scripts/seed.ts`, Node-native TS + `--env-file=.env.local`) seeds 92 demo owners (12 handcrafted + 80 generated deterministically in `scripts/seed-data.ts`, unit-tested in `tests/unit/seed-data.test.ts`) with active listings across all 12 cities, portraits and roommates; idempotent (skips existing seed emails `seed.user1..92@nestup.dev`, backfills portraits, adds roommates only to rooms without any); uses `SUPABASE_SERVICE_ROLE_KEY` from env — never hardcoded. Run against the live project 2026-08-24 (12) and 2026-08-25 (92). Demo password: Demo1234!.

- Email confirmation is ON by explicit user decision — users must click the emailed link. Confirm-signup email template in Supabase must link to `{{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type=email`.
- User-approved scope focus (2026-08-24): build order Profile → Listings → Swipe; landing polish and extras deferred. Seed data allowed as support so Browse/Swipe have content.
- Interim pages to be replaced later: `app/page.tsx` interim landing (Task 15, deferred). (The `/swipe` stub was replaced by the real deck on 2026-08-25.)
- **Same-tab navigation guard (2026-08-25):** a reported "nav opens a new tab" issue was investigated and not reproducible — every internal link is a `next/link`, the served HTML has no `target`/`<base>`/`window.open`, and a Playwright run against production showed all nav/card/logo clicks staying in one tab with client-side routing. Guards added: `tests/unit/same-tab-navigation.test.ts` (source scan: no `window.open`/`<base>`/form targets, `<Link>` never has `target`, internal `<a>` never opens a new tab, external new-tab links need `rel=noopener|noreferrer`), a BottomNav test asserting no `target`/`rel`/`onclick`, and `npm run check:nav [baseUrl]` (`scripts/check-same-tab-nav.mjs`, real Chromium via `playwright`). External Google Calendar links in `ViewingCard.tsx` intentionally keep `target="_blank"`.
- **Web app manifest (2026-08-25):** `app/manifest.ts` (`scope: "/"`, `start_url: "/"`, `display: standalone`, icons in `public/icons/`, `app/apple-icon.png`) + `appleWebApp`/`viewport.themeColor` in `app/layout.tsx`. Added after a Mac user reported nav clicks opening new browser tabs even though Chromium *and* WebKit runs against production stayed in one tab — the remaining Apple-specific cause is Safari "Add to Dock" / iOS home-screen mode, which sends out-of-scope navigations to the browser when no manifest declares the scope. Only share `https://nestup-kappa.vercel.app`: `nestup-nu18.vercel.app` and the per-deployment URLs sit behind Vercel Deployment Protection and show a Vercel login page.
- **Single typeface + profile header (2026-08-25):** Fraunces removed; Inter is the only font (`--font-serif` now resolves to Inter, all `font-serif` classes stripped). `/profile` header: `components/profile/ProfileAvatar.tsx` — 7rem picture, click → full-size lightbox (Escape/backdrop/✕ close), hover pencil badge → `/profile/edit` (always visible on touch); name · profession · Instagram-style bio (`whitespace-pre-line`); "Edit info" button removed. Own-profile-only editing is enforced at route (`/profile/edit`, no id param), action (`user_id` from session — `tests/unit/profile-action.test.ts`) and RLS (`profiles` insert/update `user_id = auth.uid()`, `avatars` bucket folder = uid).
- **Member profile pages (2026-08-25):** `/people/[id]` (`app/(app)/people/[id]/page.tsx`, signed-in only; own id → `/profile`, non-UUID/unknown → 404) shows a member's header (avatar, name/age, occupation, bio, interest chips), their About-me intro and the same groups as the About me tab (`lib/people.ts` `profileGroups` — Daily life / Habits & home / Social / Looking for, empty rows dropped, social handles turned into links via `socialHref`), plus their active listings. Roommate names link there from the listing page "Who lives here" (`browse/[id]`) and the Swipe deck's Roommates tab (`SwipePanel.tsx`). `profile_details` stays owner-only at the table level; migration `0013_public_profile_details.sql` adds `public_profile_details(uuid)` (security definer, `authenticated` only) returning the shareable subset — **phone and contact e-mail are never exposed**; chat is the contact channel. Tests: `tests/unit/people.test.ts`, link assertions in `swipe-deck.test.tsx`.
- **Known test-env issue (2026-08-25):** `tests/unit/theme-toggle.test.tsx` fails under Node 25 (`localStorage.clear is not a function`) because Node's experimental global `localStorage` shadows jsdom's in that file; unrelated to app code — run tests on Node 22/24 or stub `window.localStorage` in the test setup.
- **Type scale (2026-08-25):** all text one notch larger by user request. `app/globals.css` overrides Tailwind's `--text-*` tokens in a `@theme` block (xs 13 · sm 15 · base 17 · lg 19 · xl 22 · 2xl 26 · 3xl 32 · 4xl 40 px, line-heights adjusted) so every `text-sm`/`text-base`/… class grows without touching components; the pixel-sized eyebrow labels were bumped in place (`text-[10px]`→`11px`, `11`→`12`, `15`→`16`, `26`→`28`). Layout spacing (rem-based) is unchanged. To go bigger/smaller again, edit the tokens, not the components.
- **Read-only Profile, editing on the pencil page (2026-08-25):** behind `lib/feature-flags.ts` `PROFILE_EDIT_ON_PENCIL_PAGE` (true). On: `/profile` About-me tab renders `components/profile/AboutView.tsx` (the same read-only view `/people/[id]` uses — chips, intro, grouped details, no phone/e-mail; own view shows a "tap the pencil" hint when empty); `/profile/edit` renders `ProfileForm` with `about={{details,email}}`, which appends `AboutFields compact` (About-me inputs minus the fields the form already has) to the SAME form — `upsertProfileAction` validates them with `aboutDetailsSchema` when `formData.has("about")` and upserts `profile_details` after the profile row (tests in `profile-action.test.ts`). Off: the previous layout (inline `AboutMe` editor with its own `saveAboutAction`; pencil page basics only). `AboutMe` now wraps `AboutFields` (full mode). Flip the flag + redeploy to undo.
- **Viewing hours, approval-gated viewings, chat photos, date pickers (2026-08-25):** migration `0014_viewing_hours_chat_images.sql` — `listings.viewing_slots jsonb` (`[{day 0–6, from, to}]`, empty = any time), `messages.image_path` (content may be empty when a photo is attached), private bucket `chat-images` (`<conversation>/<uuid>.<ext>`, participant-only RLS via the conversations policy; the thread page hands out 1-hour signed URLs), trigger `viewings_guard` (only the non-proposer can approve/decline a *pending* viewing; proposals immutable), `my_conversations()` now returns `listing_viewing_slots` and previews photo-only messages as "📷 Photo". Pure helpers in `lib/availability.ts` (`normalizeSlots`, `startTimes`, `fitsAvailability` in Asia/Jerusalem, `describeSlots`). Listing form: `ViewingHoursEditor` (JSON field `viewing_slots`, validated in `saveListingAction`); listing page shows "Viewing hours". Chat: `ScheduleViewing` = inline `DatePicker` limited to the host's weekdays + time chips from `startTimes` (seekers only; hosts unrestricted; server re-checks with `fitsAvailability`); `ViewingCard` shows "Pending approval" → Approve/Decline by the other party → "Approved" unlocks "Add to Google Calendar"; Google events are created on approval from the approver's connected calendar (`mirrorToGoogle`), or later via "Send invite" (`syncViewingToGoogleAction`) — nothing reaches a calendar before approval. `MessageComposer`: photo button → browser-side compress → direct upload → preview → send; arrow-up send icon (centred). Listing photo replaces avatars in the chat header and inbox rows. `components/ui/DatePicker.tsx` (popover/inline month calendar, `min/max/allowedWeekdays/clearable`, hidden input) replaces every `type="date"`; `components/ui/Select.tsx` (`Select`, `TimeSelect`) replaces `type="time"` and plain selects in About me / viewing hours. Tests: `availability.test.ts`, `date-picker.test.tsx`, `viewing-card.test.tsx`, message schema cases in `validation.test.ts`.
- **Chat sending + viewings (2026-08-25):** `MessageComposer` is controlled (no remount) and hands `SendPayload` to `ChatThread`, which shows the bubble optimistically (`lib/chat-outbox.ts`: `mergeMessages`/`settledClientIds`) and calls `sendMessageAction({conversationId, clientId, content, imagePath})` in the background; failed sends show "Not sent · Retry · Dismiss" under the bubble. Migration 0015 adds `messages.client_id` + unique `(conversation_id, client_id)` — a retried id returns the existing row (23505), so duplicates are impossible — and extends `my_conversations()` with `next_viewing_starts_at/ends_at` (next confirmed viewing that hasn't ended). Header: the "Schedule a viewing" button is gone (the composer's calendar button remains); while a confirmed viewing is ahead the header shows a "Viewing scheduled" chip (`components/chat/ViewingDetails.tsx`: date, time, property, participants, notes, calendar link) and the chat thumbnail in both the header and the inbox gets an accent outline ring (`outline-2 outline-offset-2 outline-accent` — green in Editorial, gold in Noir) that disappears once the viewing is cancelled/declined or has ended.
- **Cancel from the header + "Scheduled" label (2026-08-25, evening):** the "Viewing scheduled" details sheet (`components/chat/ViewingDetails.tsx`) now ends with a **Cancel viewing** link → inline confirm ("Yes, cancel viewing" / "Keep it") → `respondViewingAction(id, "cancelled", conversationId)` (the same action the timeline card uses; either party may cancel a confirmed viewing, the `viewings_guard` trigger still rules). On success the sheet closes and the action's `revalidatePath` clears the chip, rings and labels; on error a `role="status"` line explains and the sheet stays open. Under the ringed thumbnail — chat header (`ChatThread.tsx`, `VIEWING_LABEL`) and inbox rows (`ConversationList.tsx`) — the word **Scheduled** appears in the same accent (green in Editorial, gold in Noir); the inbox's old `sr-only` "Viewing scheduled" text was replaced by this visible label. Tests: two new cases in `tests/unit/viewing-details.test.tsx` (147 total). Deployed to production and probed with seed.user1 (backed out via "Keep it", so the demo viewing survives).
- **Realtime fix + one open viewing per chat (2026-08-25, evening):** *Bug:* after a cancel/approval the other party's chat never updated (ring/"Scheduled" stayed) — `ChatRealtime` called `subscribe()` synchronously after `createClient()`, so the channel joined **before** the session token was attached; Realtime evaluated RLS as anonymous and delivered nothing, and supabase-js never re-sent the token (verified on production by watching the websocket: join frame without `access_token`, zero `postgres_changes` frames; a client that awaits `getSession()` first receives events). *Fix:* `components/chat/ChatRealtime.tsx` now awaits `auth.getSession()`, calls `realtime.setAuth(token)`, and only then subscribes; it also `router.refresh()`es when the tab becomes visible again. `respondViewingAction` additionally revalidates `/chat` so the actor's inbox clears too. *Rule:* one open viewing per chat — a pending proposal or a confirmed viewing that hasn't ended blocks a new request: `lib/chat-outbox.ts` `openViewing()` + `OPEN_VIEWING_MESSAGE`; `ChatThread` shows the notice instead of the sheet (composer calendar button dimmed, `scheduleBlocked`), `proposeViewingAction` re-checks server-side, and migration `0016_one_open_viewing.sql` (`viewings_one_open` BEFORE INSERT trigger, applied to the live project) enforces it for any client (probed: second insert → "A viewing is already scheduled in this chat."). Tests: `tests/unit/chat-realtime.test.tsx` (token-before-join order, debounced refresh, visibility refresh), `openViewing` cases in `chat-outbox.test.ts`. Throwaway conversation seed.user5 ↔ seed.user6's room was used for the live probes and deleted afterwards.
- **Global back link (2026-08-25, evening; redesigned same evening):** `components/ui/BackButton.tsx` renders **← Back to <page>** under the site header (top-left of the content column, `px-4 pt-4 sm:px-6`) in both the (app) and (public) layouts — styled like the Swipe panel's "Full listing →" link (`text-xs font-semibold uppercase tracking-[0.16em] text-accent`), arrow pointing left. The label names the page this tab came from via an in-app trail (`lib/back.ts`: `recordVisit` on every pathname change, `previousVisit`, `markGoingBack` so our back click / browser Back pops instead of pushing, persisted in `sessionStorage` `nestup:nav-trail`, exposed with `useSyncExternalStore` so the label is right before and after the visit is recorded); click → `router.back()` when a trail entry exists and `canGoBack()` (Navigation API, else history length), otherwise `router.push(parentPath(pathname))` and the label names that parent (`pageName`: room / listings / chats / chat / profile / edit profile / swipe / listing form). **Main tabs never show it** (`isMainPage`: `/`, `/swipe`, `/browse`, `/chat`, `/profile` — user decision 2026-08-25 evening: back only when going from a main page into a deeper one). **Chat threads don't show it either** (`showsBackLink`, user decision): on desktop the inbox sits beside the thread; on phones the thread header's own `lg:hidden` arrow (`Link` → `/chat`, aria-label "Back to chats") was restored because the bottom nav hides inside a thread. `ChatShell` therefore keeps `top-16` for the phone overlay; its desktop height is `100dvh-12.125rem` (header + top margin + floating nav) so the page never scrolls. The chat thread's own mobile-only arrow was removed. Tests: `tests/unit/back-button.test.tsx` (parentPath/pageName tables, canGoBack, trail push/pop/reload, label + click behaviour, hidden on Listings).
- **Interest chips on profiles (2026-08-26).** `AboutView` (own Profile tab and `/people/[id]`) renders interests with the same green bubble as the swipe Roommates page (`border-accent/40 bg-accent/10 font-bold text-accent`); the grey variant is only used on the swipe page for non-shared interests.
- **"My listing" tab (2026-08-26).** `ProfileTabs` labels the tab "My listing" with no count and renders `components/profile/MyListing.tsx`: no listing → one dashed "+ Add listing" square (→ `/listing`); with one → every photo as a square in the profile grid, room caption when tagged, a hover/touch pencil per photo → `/listing#photos` (the form's Photos section now has `id="photos"` + `scroll-mt-24`). The old "Edit listing" tile and the "No listings yet" empty state are gone; Liked/History keep their grids and counts. `PencilIcon` moved to `components/ui/PencilIcon.tsx` (shared with `ProfileAvatar`). Tests: `tests/unit/my-listing.test.tsx`.
- **Theme switch (2026-08-26).** `components/ui/ThemeToggle.tsx` is a sliding switch (`role="switch"`, `aria-label="Dark mode"`, `title` says which way it flips): grey track + white knob with a sun in light, accent track + surface knob with a moon in dark; knob moves with `translate-x-5` (Tailwind v4 `translate`, not `transform`). Same persistence as before (`<html data-theme>` + `localStorage.theme`). Probes should use `getByRole("switch", { name: /dark mode/i })` — the "Dark"/"Light" text pill is gone.
- **Login page: forgot password, eye toggle, header (2026-08-26).** `app/(auth)/layout.tsx` (new) gives login/signup/forgot/reset the wordmark (→ `/`) and the `ThemeToggle` in a header — the group previously rendered bare. `components/auth/PasswordInput.tsx` wraps every password field with an eye button (`aria-label` "Show password"/"Hide password", `aria-pressed`, `type="button"`), shared field classes live in `components/auth/fields.ts`. Login shows **Forgot your password?** → `/forgot-password` (`ForgotPasswordForm` + `requestPasswordResetAction`: `resetPasswordForEmail(email, { redirectTo: <origin>/auth/confirm?next=/reset-password })`, origin from `NEXT_PUBLIC_SITE_URL` else the request host; **always answers "sent"** except on 429, so the form can't enumerate accounts). `app/auth/confirm/route.ts` now accepts both link shapes — `token_hash&type` (`verifyOtp`; `type=recovery` → `/reset-password`, `type=email` → onboarding as before) and `code&next=/reset-password` (Supabase's default `{{ .ConfirmationURL }}` bouncing to `redirectTo`; `exchangeCodeForSession`) — and sends bad recovery links to `/login?error=recovery` (login notices table in `login/page.tsx`). `/reset-password` (signed-in only, else `/login?error=recovery`) renders `ResetPasswordForm` (new + confirm, both with eyes) → `updatePasswordAction` (≥8, match, `auth.getUser()` must exist, `updateUser({password})`, `same_password` explained) → `/swipe`. **Ops prerequisite:** the Vercel domain must be in Supabase Auth Redirect URLs (see HANDOVER ops item) — until then the emailed link goes to the Site URL. Tests: `password-input.test.tsx`, `auth-form.test.tsx`, `auth-confirm-route.test.ts`, `auth-password-actions.test.ts` (node env).
- **Balcony icon (2026-08-26).** `DetailIcon` "balcony" redrawn as a French door above a railing on a floor slab (the old five-bar-plus-arch glyph read as a gate). Same 24-box / 1.5 stroke as the rest of the set; shows on the room page amenities and the Swipe Home tab.
- **Balcony icon = owner artwork (2026-08-26).** Superseded the traced glyph: `DetailIcon` renders "balcony" as a currentColor CSS mask of `public/icons/balcony.png` (320×180, extracted from `my listing-guy/Balcony-icon.jpeg` — dark pixels → alpha, trimmed), same technique as `Logo`. `PATHS` is typed `Exclude<DetailIconName, "balcony">`. Re-extract with sharp if the source file changes.
